# Lions Academie frontend env template.
# Copy to `.env.local` and override per environment.

# Base URL of the Laravel API (no trailing slash).
# Dev default points at `php artisan serve`.
VITE_API_URL=http://127.0.0.1:8000
